# Local dev — proxied through Vite to localhost:5001
VITE_API_URL=

# Production web / Capacitor native build — MUST be an absolute URL
# Native apps cannot use relative URLs — set this before running: npm run cap:build
# VITE_API_URL=https://lockin-backend.onrender.com
